# Build-time public env vars baked into the static output by Astro.
# These values are visible in the deployed HTML (GA tag, AdSense client),
# so they're safe to commit. They are NOT secrets.
PUBLIC_GA_ID=G-459FX52RV3
PUBLIC_ADSENSE_CLIENT_ID=ca-pub-1069480025527043
